# ===== DATABASE CONFIGURATION =====
# Local development database settings (using Docker)
DB_HOST=localhost
DB_PORT=5544
DB_NAME=hiresync_db
DB_USERNAME=hiresync_user
DB_PASSWORD=hiresync_password

# Production database settings (for Render deployment)
# Use Render-provided values in dashboard instead of uncommenting these
# JDBC_DATABASE_URL=jdbc:postgresql://internal-db-url/database_name
# JDBC_DATABASE_USERNAME=your_render_db_username
# JDBC_DATABASE_PASSWORD=your_render_db_password

# ===== JWT CONFIGURATION =====
JWT_SECRET=your-jwt-secret-key-should-be-at-least-256-bits-long
JWT_EXPIRATION=86400000

# ===== APPLICATION CONFIGURATION =====
PORT=8080

# ===== POOL CONFIGURATION =====
DB_MAX_POOL_SIZE=10
DB_MIN_IDLE=5

# ===== JPA CONFIGURATION =====
# Use 'update' for development, 'validate' for production
JPA_DDL_AUTO=update

# ===== LOGGING LEVELS =====
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=INFO
LOG_LEVEL_SQL=INFO

# ===== ENVIRONMENT NOTES =====
# For local development:
# - Keep DB_HOST as localhost to connect to containerized database through port-forwarding
#
# For Docker Compose Production:
# - Set DB_HOST=postgres when running with docker-compose.prod.yaml
# - Set DB_PORT=5432 when running with docker-compose.prod.yaml
#
# For Render deployment:
# - Set environment variables in Render dashboard
# - Render automatically sets PORT
# - Use the database connection details from your Render PostgreSQL instance 